# 📊 Modules Feature - Architecture & Flow Diagrams

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────┐
│                    FLUTTER APP                      │
├─────────────────────────────────────────────────────┤
│                                                     │
│  ┌────────────────────────────────────────────┐   │
│  │         HOME SCREEN (home_screen.dart)    │   │
│  ├────────────────────────────────────────────┤   │
│  │ Welcome!                                   │   │
│  │ ┌──────────────┬──────────────┐            │   │
│  │ │ 👤 Profile   │ 📚 Modules   │            │   │
│  │ └──────┬───────┴──────┬───────┘            │   │
│  │        │              │                    │   │
│  │        ↓              ↓                    │   │
│  │   ProfileScreen   ModulesListScreen       │   │
│  │                  (modules_list_screen.dart)│   │
│  │                  ┌─────────────────┐      │   │
│  │                  │ All Modules     │      │   │
│  │                  │ ┌─────────────┐ │      │   │
│  │                  │ │ Module 1    │ │      │   │
│  │                  │ │ • Course A  │ │      │   │
│  │                  │ │ • Course B  │ │      │   │
│  │                  │ └─────────────┘ │      │   │
│  │                  └─────────────────┘      │   │
│  └────────────────────────────────────────────┘   │
│                         ↓                         │
│          ┌──────────────────────────────┐        │
│          │   Module Service             │        │
│          │ (module_service.dart)        │        │
│          │                              │        │
│          │ Handles data operations      │        │
│          └──────────────────────────────┘        │
│                         ↓                         │
│         ┌───────────────────────────────┐        │
│         │   Supabase Client             │        │
│         │ (Remote Database)             │        │
│         └───────────────────────────────┘        │
│                         ↓                         │
└─────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────┐
│                  SUPABASE                          │
├─────────────────────────────────────────────────────┤
│                                                     │
│  ┌────────────────────────────────────────────┐   │
│  │          PostgreSQL Database               │   │
│  │                                            │   │
│  │  ┌──────────────┐    ┌──────────────┐    │   │
│  │  │  modules     │    │  courses     │    │   │
│  │  ├──────────────┤    ├──────────────┤    │   │
│  │  │ id (PK)      │    │ id (PK)      │    │   │
│  │  │ title        │    │ title        │    │   │
│  │  │ description  │    │ description  │    │   │
│  │  │ course_id    │───→│ id (FK)      │    │   │
│  │  │ order        │    │ module_id    │    │   │
│  │  │ created_at   │    │ created_at   │    │   │
│  │  │ updated_at   │    │ updated_at   │    │   │
│  │  └──────────────┘    └──────────────┘    │   │
│  │                                            │   │
│  └────────────────────────────────────────────┘   │
│                                                     │
└─────────────────────────────────────────────────────┘
```

## 📱 Navigation Flow Diagram

```
┌──────────────────┐
│   App Startup    │
└────────┬─────────┘
         │
         ↓
┌──────────────────┐
│  Auth Check      │
└────────┬─────────┘
         │
         ↓ (Logged In)
┌──────────────────┐
│  Home Screen     │
│                  │
│  ┌────┐  ┌────┐  │
│  │👤  │  │📚  │  │
│  │Prof│  │Mod │  │
│  └─┬──┘  └─┬──┘  │
│    │       │     │
└────┼───────┼─────┘
     │       │
     │       ↓ (User taps Modules icon)
     │       
     │   ┌─────────────────────────┐
     │   │ ModulesListScreen       │
     │   │ (modules_list_screen.dart)
     │   │                         │
     │   │ FutureBuilder starts    │
     │   │ Fetches from Supabase   │
     │   │                         │
     │   │ Loading: Shows Spinner  │
     │   │ Error: Shows Error      │
     │   │ Empty: Shows Message    │
     │   │ Data: Shows Modules     │
     │   │                         │
     │   │ Module 1 Card           │
     │   │ Module 2 Card           │
     │   │ Module 3 Card           │
     │   │ ...                     │
     │   │                         │
     │   └─────────────────────────┘
     │              ↓
     │        ┌─────────────┐
     │        │ User Scrolls│
     │        │ or Goes Back│
     │        └────────┬────┘
     │               │
     └───────────────┘
         ↓ (Back to Home)
    Home Screen
```

## 🔄 Data Flow Diagram

```
User Interaction Layer
┌─────────────────────────────┐
│  User taps "Modules" card   │
└────────────┬────────────────┘
             │
             ↓
UI Component Layer
┌─────────────────────────────┐
│ ModulesListScreen           │
│ • onInit: _fetchModules()   │
│ • setState updates UI        │
│ • ListView displays data     │
└────────────┬────────────────┘
             │
             ↓
Business Logic Layer
┌─────────────────────────────┐
│ _fetchModules() method      │
│ Calls Supabase API          │
│ .select('*, courses(*)')    │
│ .order('order', ascending)  │
│ Maps response to Models     │
└────────────┬────────────────┘
             │
             ↓
API/Network Layer
┌─────────────────────────────┐
│ Supabase Client             │
│ • Connects to backend       │
│ • Executes query            │
│ • Returns JSON data         │
└────────────┬────────────────┘
             │
             ↓
Database Layer
┌─────────────────────────────┐
│ PostgreSQL Database         │
│ SELECT modules with courses │
│ FROM modules               │
│ JOIN courses               │
│ ORDER BY order             │
└────────────┬────────────────┘
             │
             ↓ (Returns Data)
Database Layer
┌─────────────────────────────┐
│ JSON Response               │
│ [                           │
│   {                         │
│     id: "...",             │
│     title: "Module 1",     │
│     courses: [             │
│       { id: "...", title }  │
│     ]                       │
│   }                         │
│ ]                           │
└────────────┬────────────────┘
             │
             ↓
API Layer (Response)
┌─────────────────────────────┐
│ Data returned to client     │
└────────────┬────────────────┘
             │
             ↓
Business Logic
┌─────────────────────────────┐
│ Parse JSON                  │
│ Create Module objects       │
│ Set state with data         │
└────────────┬────────────────┘
             │
             ↓
UI Component
┌─────────────────────────────┐
│ Rebuild widgets             │
│ Render module cards         │
│ Display courses in each     │
│ Show to user                │
└─────────────────────────────┘
```

## 🏭 Component Hierarchy

```
                    App
                    │
            ┌───────┼───────┐
            │       │       │
         Main  MaterialApp  Home
                    │
            ┌───────┴───────┐
            │               │
        Routes         Initial Route
            │               │
         home    ────────  HomeScreen
            │                   │
        modules_list   ─────  ModulesListScreen
                            │
                    ┌───────┴────────┐
                    │                │
                 AppBar          Body
                    │                │
              All Modules        ListView
                                     │
                            ┌────────┴────────┐
                            │                 │
                       Loading           ModuleCard
                       Error           ┌────┴────┐
                       Empty           │         │
                                    Header    Content
                                       │         │
                                   Title      Courses
                                 Desc      Metadata
```

## 📊 State Management Flow

```
ModulesListScreen (StatefulWidget)
        │
        ├─ State
        │  ├─ _modulesFuture: Future<List<Module>>
        │  ├─ supabase: SupabaseClient
        │  └─ Methods
        │     ├─ initState()
        │     │  └─ _modulesFuture = _fetchModules()
        │     │
        │     ├─ _fetchModules()
        │     │  ├─ Query Supabase
        │     │  ├─ Parse response
        │     │  └─ Return List<Module>
        │     │
        │     └─ build()
        │        └─ FutureBuilder
        │           ├─ connectionState.waiting → Loading
        │           ├─ snapshot.hasError → Error
        │           ├─ data.isEmpty → Empty
        │           └─ data.isNotEmpty → Success
        │
        └─ UI Tree
           ├─ Scaffold
           │  ├─ AppBar
           │  └─ Body
           │     ├─ FutureBuilder
           │     │  └─ ListView
           │     │     └─ ModuleCard (repeating)
           │     │        ├─ Header
           │     │        │  ├─ Module Number
           │     │        │  ├─ Title
           │     │        │  └─ Description
           │     │        │
           │     │        ├─ Content
           │     │        │  ├─ Course Count
           │     │        │  └─ Course List
           │     │        │     └─ Course Item (repeating)
           │     │        │
           │     │        └─ Footer
           │     │           ├─ ID
           │     │           └─ Created Date
           │     │
           │     └─ Special States
           │        ├─ Loading → CircularProgressIndicator
           │        ├─ Error → Error widget
           │        └─ Empty → Empty state widget
```

## 🔌 Integration Points

```
┌──────────────────────────────────────────────┐
│              FLUTTER APP                     │
├──────────────────────────────────────────────┤
│                                              │
│  Home Screen                                 │
│  └─ Feature Card (Modules)                   │
│     └─ Navigation callback                   │
│        └─ Navigator.push()                   │
│           └─ Route to ModulesListScreen      │
│              │                               │
│              ├─ Supabase Client              │
│              │  └─ REST API calls            │
│              │     └─ HTTP to backend        │
│              │                               │
│              ├─ Module Model                 │
│              │  └─ Handles data structure    │
│              │                               │
│              ├─ ModuleService                │
│              │  └─ Business logic            │
│              │                               │
│              └─ UI Components                │
│                 └─ Widgets rendering         │
│                                              │
└──────────────────────────────────────────────┘
         ↓ (HTTP/API Calls)
┌──────────────────────────────────────────────┐
│            SUPABASE BACKEND                  │
├──────────────────────────────────────────────┤
│ PostgreSQL DB                                │
│ ├─ modules table                             │
│ ├─ courses table                             │
│ ├─ RLS Policies                              │
│ └─ Realtime Subscriptions (optional)         │
└──────────────────────────────────────────────┘
```

## 🔄 Sequence Diagram

```
User          App          Supabase
 │             │               │
 ├─Tap Modules─→              │
 │             │               │
 │             ├─Navigate──────→
 │             │  ModulesListScreen
 │             │               │
 │             ├─Show Loading──→
 │             │  (spinner)    │
 │             │               │
 │             ├─Query: GET───→┤
 │             │  modules      │
 │             │  +courses     │
 │             │               │
 │             │←─JSON Data───┤
 │             │               │
 │             ├─Parse Data───→
 │             │  Create Models
 │             │               │
 │             ├─Render UI────→
 │             │               │
 ├─See Modules─│               │
 │             │               │
 ├─Scroll────→ │               │
 │             ├─More data?───→
 │             │  (paginated) │
 │             │               │
 ├─Go Back────→ │               │
 │             │               │
 │  ←─Home Screen───────────────
 │             │               │
```

## 📈 Data Transformation

```
Raw JSON from Supabase
        ↓
┌───────────────────┐
│ Map<String, dynamic>
│ {
│   "id": "abc123",
│   "title": "Module 1",
│   "courses": [
│     {"id": "123", "title": "Course 1"}
│   ]
│ }
└───────────────────┘
        ↓
┌───────────────────┐
│ Module.fromMap()
│ Constructor
│ Parsing & Validation
└───────────────────┘
        ↓
┌───────────────────┐
│ Module Object
│ id: String
│ title: String
│ courses: List<Course>
│ ...
└───────────────────┘
        ↓
┌───────────────────┐
│ ModuleCard Widget
│ Renders with data
│ Shows in ListView
└───────────────────┘
        ↓
┌───────────────────┐
│ User Interface
│ Beautiful cards
│ with info
└───────────────────┘
```

## 🎨 Widget Tree

```
Scaffold
├─ AppBar
│  └─ Text("All Modules")
│
└─ Body: FutureBuilder
   ├─ Loading State
   │  └─ CircularProgressIndicator
   │
   ├─ Error State
   │  └─ Column
   │     ├─ Icon (error)
   │     └─ Text (error message)
   │
   ├─ Empty State
   │  └─ Column
   │     ├─ Icon (empty)
   │     ├─ Text (no modules)
   │     └─ Text (friendly msg)
   │
   └─ Success State
      └─ ListView
         └─ For each module:
            └─ _ModuleCard
               ├─ Container (header)
               │  ├─ Chip (module number)
               │  ├─ Text (title)
               │  └─ Text (description)
               │
               ├─ Padding (content)
               │  ├─ Row (course count)
               │  │  ├─ Icon
               │  │  └─ Text
               │  │
               │  └─ For each course:
               │     └─ Row
               │        ├─ Circle (bullet)
               │        └─ Column
               │           ├─ Text (course title)
               │           └─ Text (course desc)
               │
               └─ Container (footer)
                  └─ Row
                     ├─ Text (ID)
                     └─ Text (date)
```

---

**All diagrams show the complete architecture and flow of the Modules feature.**
